{
  "name": "other-hover",
  "version": "1.0.3",
  "description": "Toggle classes on DOM elements when hovering over other elements",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/marcjfj/other-hover.git"
  },
  "scripts": {
    "dev": "esbuild index.js --outfile=dist/index.js",
    "build": "esbuild index.js --minify --sourcemap --target=es2015  --outfile=dist/index.js",
    "test": "jest --verbose"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "babel-jest": "^26.6.3",
    "esbuild": "^0.8.26",
    "jest": "^26.6.3"
  }
}